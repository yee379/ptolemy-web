<%= form_for(@weathermap) do |f| %>
  <% if @weathermap.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@weathermap.errors.count, "error") %> prohibited this weathermap from being saved:</h2>

      <ul>
      <% @weathermap.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
